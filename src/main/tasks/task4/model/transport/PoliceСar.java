package tasks.task4.model.transport;

import tasks.task4.model.people.Policeman;

public class PoliceСar extends Car<Policeman>{
    public PoliceСar(Integer capacity) {
        super(capacity);
    }
}
